# Dashboard env — copy to .env.local before running `pnpm dev`.
# Neither var is `NEXT_PUBLIC_*`, so they live only on the Next.js server.
PLATFORM_API_URL=http://localhost:8000
PLATFORM_API_KEY=change-me
